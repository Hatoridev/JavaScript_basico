// Seleciona o botão pelo ID
const btnInfo = document.getElementById("btnInfo");

// Adiciona um evento de clique ao botão
btnInfo.addEventListener("click", function () {
  // Exibe um alerta com mais informações
  alert("Os navegadores modernos possuem diversas responsabilidades, incluindo segurança, renderização de conteúdo e execução de scripts.");
});

